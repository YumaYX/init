---
# tasks file for roles/more
- name: Add Google Chrome repository
  ansible.builtin.yum_repository:
    name: google-chrome
    description: Google Chrome
    baseurl: https://dl.google.com/linux/chrome/rpm/stable/x86_64
    enabled: true
    gpgcheck: true
    gpgkey: https://dl.google.com/linux/linux_signing_key.pub

- name: Install Google Chrome
  dnf:
    name: google-chrome-stable
    state: present

- name: Download and install Ollama
  shell: |
      curl -fsSL https://ollama.com/install.sh | sh
- name: Enable and start Ollama service
  systemd:
    name: ollama
    enabled: yes
    state: started
    daemon_reload: yes

- name: Git clone # noqa latest[git]
  ansible.builtin.git:
    repo: https://github.com/YumaYX/terminal-design.git
    dest: /home/{{ prime_user }}/terminal-design
  become: true
  become_user: "{{ prime_user }}"

- name: Run 'load' target as {{ prime_user }}
  community.general.make:
    chdir: /home/{{ prime_user }}/terminal-design
    target: load
  become: true
  become_user: "{{ prime_user }}"
